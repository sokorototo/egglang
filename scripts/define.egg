do(
	# Defines a new variable
	define(toilet, "STINKS"),
	print(toilet),

	# You can't redefine a variable
	# -- CAUSES ERROR -- define(toilet, "STINKS")
	# You can only use 'mutate' to change a variable's value
	mutate(toilet, "STINKS A LOT MORE"),
	print(toilet),
)
