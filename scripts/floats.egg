# Simple Tests for float parsing and handling
define(x, 5E-1) # 0.5
define(res, "20")
set(x, multiply(-10 x 2E2))

# Print Result
assert(equals(x, -1000), println("Float Arithmetic Test Failed: ", x))

# Simple Tests for float parsing and handling
repeat(20, set(x, sum(3.5 1E-3 5 -2.0 314159E-5 x)))
println("The value of x is: ", x)
